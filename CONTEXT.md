# MediaForge 项目上下文

## 产品定位

MediaForge 是面向培训机构和本地门店的 AI 公众号内容生成与排版平台。用户通过工作区沉淀机构或门店资料、素材、排版偏好和历史内容，再用 AI 生成、调整并复制到微信公众号后台。

第一版不直接对接微信公众号发布能力，重点做好复制粘贴、历史快照、图片识别、排版 skills 和长期学习。

## 核心用户

| 用户 | 典型场景 |
| --- | --- |
| 培训机构校区负责人 | 招生宣传、公开课、课程介绍、学员案例 |
| 课程顾问或教务 | 家长沟通、活动通知、报名引导 |
| 门店老板或店长 | 促销活动、新品推荐、会员福利、门店介绍 |
| 兼职运营人员 | 快速产出公众号图文并复制发布 |

## 技术栈

| 层级 | 当前选择 |
| --- | --- |
| 前端 | Next.js / React / TypeScript |
| 后端服务 | TypeScript 服务，当前骨架位于 `apps/service` |
| 共享契约 | `packages/contracts` |
| 数据库 | PostgreSQL |
| 对象存储 | 本地 MinIO，线上保持 S3 兼容 |
| 缓存和队列 | Redis |
| 编辑器方向 | Tiptap / ProseMirror |
| AI 集成 | Model Gateway |

## 术语表

| 术语 | 英文名 | 含义 |
| --- | --- | --- |
| 租户 | Tenant | 一个培训机构、门店主体或团队边界 |
| 用户 | User | 登录并使用系统的人 |
| 品牌上下文 | Brand Context | 机构、校区或门店的品牌资料、受众和长期偏好；不等于历史会话 |
| 创作空间 / 会话 | Conversation | 用户可见的一次创作上下文和历史记录，也是最小删除单元 |
| 素材 | Asset | 用户上传的图片、二维码、海报等资源 |
| 文章 | Article | 一篇公众号内容草稿 |
| 文章版本 | ArticleVersion | 每次 AI 生成、AI 修改或恢复形成的版本 |
| 内容 JSON | Content JSON | 可编辑的结构化文章源数据 |
| HTML 快照 | HTML Snapshot | 历史版本可复现的公众号 HTML 结果 |
| 排版技能包 | Layout Skill Pack | 行业、场景、结构、视觉和 prompt 的组合规则 |
| 工作区记忆 | Workspace Memory | 从用户配置和行为中总结出的长期偏好 |
| 模型网关 | Model Gateway | 统一管理不同模型供应商和调用策略的服务层 |

## 领域不变量

- 未发送的空会话只存在于前端，不创建后端记录。
- 用户首次发送成功后才创建 Conversation，首条 prompt 作为历史标题。
- Conversation 历史按最后一次成功用户交互时间排序。
- Conversation 是消息、资源、任务、产物和文章数据的最小删除单元。
- 品牌上下文只提供机构、门店或固定公众号场景的长期配置，不创建默认 Conversation。
- 正式文章版本必须同时保存结构化 JSON 和 HTML 快照。
- 图片原文件必须存在对象存储中，数据库只保存元数据和识别结果。
- PostgreSQL 是业务事实数据源。
- Redis 不保存不可丢失的业务事实。
- AI prompt 快照需要可追溯，但不应暴露密钥或敏感配置。
- 用户长期记忆需要可关闭、可清空、可纠错。

## 命名约定

- 后端目录命名为 `apps/service`，不使用 `apps/api`。
- 前端目录命名为 `apps/web`。
- 共享契约目录命名为 `packages/contracts`。
- 数据库字段使用 snake_case。
- TypeScript 类型、接口和枚举使用英文。
- 中文文档中首次出现核心概念时，尽量同时给出英文名。
