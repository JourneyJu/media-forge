# L 级改动计划：[标题]

## 需求背景

说明用户要解决的问题、业务目标和为什么属于 L 级。

## 分级结论

```text
需求分级：L
分级理由：
影响面：
是否需要人工确认：是
```

## 非目标

列出本次明确不做的内容，避免实现扩散。

## 影响面

| 领域 | 是否影响 | 说明 |
| --- | --- | --- |
| 前端 | 是 / 否 |  |
| 后端服务 | 是 / 否 |  |
| 共享契约 | 是 / 否 |  |
| 数据库 schema | 是 / 否 |  |
| 对象存储 | 是 / 否 |  |
| Redis / 队列 | 是 / 否 |  |
| AI 调用链路 | 是 / 否 |  |
| 权限 / 安全 | 是 / 否 |  |
| 部署 / 环境变量 | 是 / 否 |  |
| 文档 | 是 / 否 |  |

## 方案设计

说明推荐方案、关键流程、数据流和模块边界。

## 契约与数据变更

说明需要修改的 contracts、数据库表、迁移、对象存储路径或事件格式。

## allowed_files 草案

```yaml
allowed_files:
  - path/to/file
verification:
  - pnpm typecheck
```

## 实施步骤

1. 
2. 
3. 

## 验证方案

列出类型检查、单测、集成测试、手动验证和回归范围。

## 迁移与回滚

说明数据迁移方式、兼容策略、失败回滚方案。

## 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
|  |  |  |

## AI 自审

```text
AI 自审结论：通过 / 不通过 / 信息不足
分级复核：
服务边界：
契约与数据：
异常路径：
安全风险：
测试方案：
反方意见：
需要人工重点看的问题：
```

## 人工确认

```text
审核结论：待确认 / 已确认 / 驳回
确认人：
确认时间：
备注：
```

